# abort script on any command that exits with a non zero value
# See for original instructions: http://redis.io/download
set -e

tar xzf redis/redis-2.8.6.tar.gz

cd redis-2.8.6

make
make PREFIX=${BOSH_INSTALL_TARGET} install
